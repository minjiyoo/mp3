<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.common.family">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BROADCAST_STICKY" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Loading" />
        <activity android:name=".GalleryActivity" />
        <activity
            android:name=".TalkActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".BoardInputActivity" />
        <activity android:name=".Board_out" />
        <activity android:name=".MiniWebActivity" />
        <activity android:name=".WriteBoardActivity"></activity>
    </application>

</manifest>